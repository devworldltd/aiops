{
  "name": "e2e-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright E2E 테스트 (claude-ai-devops 골격). dev-e2e 에이전트가 대상 레포에 배치/병합한다.",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
